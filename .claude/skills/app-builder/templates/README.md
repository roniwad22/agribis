# App Builder Templates

Templates are generated on first successful build and saved here for reuse.
This directory starts empty. That's correct.

When `/app-builder` builds a new project type successfully, it saves the structure here as a reusable template.
